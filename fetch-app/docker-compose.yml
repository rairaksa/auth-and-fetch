version: '3'
services:
  fetch.test:
    build:
      context: ./docker
      dockerfile: dev.dockerfile
      args:
        WWWGROUP: '${WWWGROUP}'
    image: jds/fetch-apps
    ports:
      - '${APP_PORT:-80}:9000'
    environment:
      WWWUSER: '${WWWUSER}'
    volumes:
      - '.:/var/www'
    networks:
      - jds
networks:
  jds:
    driver: bridge
